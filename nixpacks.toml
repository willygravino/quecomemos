[phases.setup]
nixPkgs = [
  "python312Full",  # incluye pip
  "freetype",
  "lcms2",
  "libjpeg",
  "libpng",
  "libwebp",
  "openjpeg",
  "libtiff",
  "zlib"
]

[phases.install]
cmds = ["python3.12 -m pip install -r requirements.txt"]
