[phases.setup]
nixPkgs = [
  "python311",  # Python 3.11
  "python3.11Full",  # Instala la versión completa de Python 3.11
  "freetype",  # Fuentes
  "lcms2",  # Gestión de color
  "libjpeg",  # Imágenes JPEG
  "libpng",  # Imágenes PNG
  "libwebp",  # Imágenes WebP
  "openjpeg",  # JPEG2000
  "libtiff",  # Imágenes TIFF
  "zlib"  # Compresión
]

[phases.install]
cmds = [
  "python3.11 -m ensurepip --upgrade",  # Aseguramos que pip esté instalado
  "python3.11 -m pip install --upgrade pip setuptools",  # Actualizamos pip y setuptools
  "python3.11 -m pip install -r requirements.txt"  # Instalamos las dependencias
]
